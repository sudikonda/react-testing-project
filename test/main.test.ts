import {it, expect, describe } from 'vitest';

describe('main', () => {
    it('should be true', () => {
        expect(true).toBe(true);
    });

    it('should be false', () => {
        expect(false).toBe(false);
    });
})